export type AutoCompleteItem = {
  id: string;
  name: string;
};

export type Character = {
  name: string;
  type: string;
  ability: string;
  flavor_text: string;
  icon_url: string;
};

export type Player = {
  id: string;
  name: string;
};
